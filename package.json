{
  "name": "community-con",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "npm -w apps/web run dev",
    "build": "npm -w apps/web run build",
    "preview": "npm -w apps/web run preview",
    "test": "npm -w apps/web run test && npm -w packages/db run test",
    "typecheck": "npm -w apps/web exec tsc -- --noEmit && npm -w apps/api exec tsc -- --noEmit",

    "_comment": "--- Backend. Not needed for the design preview; see README. ---",
    "dev:api": "npm -w apps/api run dev",
    "test:api": "npm -w apps/api run test",
    "db:reset:local": "npm -w apps/api run db:reset:local",
    "db:seed:local": "npm -w apps/api run db:seed:local",
    "db:reset-seed:local": "npm -w apps/api run db:reset-seed:local"
  }
}
